---
phase: 13-backend-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - render.yaml
  - Procfile
  - server/index.js
  - server/.env.example
autonomous: true
---

# Plan 13-01: Render Deployment Configuration

Configure the Express backend for production deployment on Render.

## Tasks

### Task 1: Create render.yaml
Render Blueprint config with:
- Service type: web, Node env
- Region: Singapore (closest to HK)
- Build: npm install (frontend) + cd server && npm install (backend) + npm run build (frontend build)
- Start: node server/index.js
- Health check: /api/health
- Env vars: NODE_ENV=production, DISABLE_AUTO_CRAWL=true, sensitive keys synced manually

### Task 2: Create Procfile
web: node server/index.js

### Task 3: Update server/index.js
- Serve ../dist/ as static files in production
- SPA fallback: all non-API GETs serve index.html
- Make autoCrawl opt-in via DISABLE_AUTO_CRAWL env var

### Task 4: Update server/.env.example
Add DISABLE_AUTO_CRAWL, NVIDIA_API_KEY, AGNES_API_KEY, TURSO_DB_URL, TURSO_DB_TOKEN docs
