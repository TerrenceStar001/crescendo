---
phase: 4
slug: writing-module
status: draft
shadcn_initialized: false
preset: none
created: 2026-06-25
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for frontend phases. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (custom CSS) |
| Preset | not applicable |
| Component library | none (all hand-built React components) |
| Icon library | none (inline emoji/Unicode characters) |
| Font | `system-ui, -apple-system, sans-serif` (body); `Georgia, 'Times New Roman', serif` (DSE passage text only) |

**Source:** Inherited from existing project — no shadcn. All design tokens from `:root` CSS custom properties in `src/App.css`.

---

## Spacing Scale

Declared values (existing CSS custom properties — must use these only):

| Token | Value | Usage |
|-------|-------|-------|
| --space-1 | 4px | Icon gaps, inline padding, tight element spacing |
| --space-2 | 8px | Compact element spacing, small gaps |
| --space-4 | 16px | Default element spacing, card padding |
| --space-5 | 24px | Section padding, button padding (X axis) |
| --space-6 | 32px | Layout gaps, major section padding |
| --space-8 | 48px | Page-level spacing, large section margins |

Exceptions:

1. **Ruled-line editor line height**: 28px (non-standard — emulates DSE answer booklet ruled spacing). Use explicit `line-height: 28px` on `.writing__editor--exam` — do NOT use a spacing token.
2. **DSE exam header padding**: 8px top/bottom padding (existing `.reading__exam-framework` pattern uses explicit `8px` — match this).
3. **Timer warning flash**: Transition duration of 400ms for the warning pulse animation when timer drops below thresholds — overrides the standard 150ms transition.
4. **--space-3 value**: 12px is not in the standard scale {4, 8, 16, 24, 32, 48, 64} but is a valid 4-grid value (12 = 4×3). Use 12px for label-to-content gaps and small button padding when 8px is too tight and 16px is too loose. This is a one-off exception, not a new scale tier.

**Source:** Existing CSS custom properties from `:root` / `html[data-theme="dark"]` in `src/App.css` lines 51-57.

---

## Typography

**4 sizes, 2 weights maximum** (enforced by design system constraints):

| Role | Size | Weight | Line Height | CSS Token |
|------|------|--------|-------------|-----------|
| Caption / Small print | 11.2px (0.7rem) | 400 | 1.4 | `--font-xs` |
| Label / Meta / Body / Prompt | 16px (1rem) | 400 | 1.5 | `--font-base` |
| Heading / Timer | 24px (1.5rem) | 700 | 1.2 | `--font-xl` |
| Display (module title) | 32px (2rem) | 700 | 1.15 | `--font-2xl` |

**Size 16px (`--font-base`) is the catch-all** — used for: body text, ruled-line editor, prompt card task, subheadings, labels, meta text, and small print emphasis. Avoid creating separate sizes for visually close roles. Line-height may vary per component (e.g., editor uses 28px line-height for ruled-line spacing) but font-size stays at 16px.

**Weight 700 is the emphasis weight** — used for: headings, display, timer, labels needing emphasis, error chart labels. Weight 400 covers everything else. No 600 weight.

**New writing-specific typography rules:**

- **DSE exam header**: 11.2px (0.7rem), weight 700, centered, uppercase letter-spacing 0.04em — match existing `.reading__exam-title` pattern.
- **Ruled-line editor text**: 16px (1rem), weight 400, line-height 28px for ruled-line spacing — write as explicit value.
- **Prompt card task text**: 16px (1rem), weight 400, line-height 1.65 — uses `--font-base`, no separate size.
- **Error correction text in annotations**: 11.2px (0.7rem), weight 400, line-height 1.5 — uses `--font-xs`.
- **Error chart labels**: 11.2px (0.7rem), weight 700, uppercase — uses `--font-xs` with weight 700.

**Source:** Existing CSS tokens from `src/App.css` lines 45-49. Additional writing-specific specs derived from existing `.writing__*` styles and CONTEXT.md D-27 through D-36.

---

## Color

| Role | Value (Light) | Value (Dark) | Usage |
|------|---------------|---------------|-------|
| Dominant (60%) | `#f7f5f0` | `#121220` | Background surfaces (`--color-canvas`) |
| Secondary (30%) | `#ffffff` | `#1c1c30` | Cards, sidebar, prompt panel, editor background (`--color-surface`) |
| Secondary soft | `#f0ece6` | `#222238` | Alternate surface, DSE booklet area (`--color-surface-soft`) |
| Accent (10%) | `#5b5bf0` | `#8888f8` | Primary buttons, interactive elements, links (`--color-accent`) |
| Accent hover | `#4848d0` | `#aaaaff` | Button hover states (`--color-accent-hover`) |
| Accent bg | `rgba(91, 91, 240, 0.08)` | `rgba(136, 136, 248, 0.12)` | Subtle accent backgrounds (`--color-accent-bg`) |
| Success | `#4caf50` | `#66bb6a` | Correct answers, positive indicators, "pitfalls avoided" |
| Warning | `#ff9800` | `#ffb74d` | Medium scores, timer warning thresholds |
| Error | `#ef5350` | `#ef5350` | Destructive actions, wrong answers, grammar errors |
| Border | `rgba(0,0,0,0.06)` | `rgba(255,255,255,0.06)` | Subtle dividers (`--color-border`) |
| Border strong | `rgba(0,0,0,0.12)` | `rgba(255,255,255,0.12)` | Strong dividers (`--color-border-strong`) |

**Accent reserved for:** Primary CTA buttons (submit, start session, practice again), active tab indicators, accent backgrounds for Part A badge (same as Reading Module's `.reading__passage-part-badge--A`), interactive focus rings, inline annotation icons. Never used for passive surface backgrounds.

**Part B option cards:** Use `.reading__difficulty-card` pattern (white surface, border, no accent color until hover).

**Error type color coding for inline annotations (D-23):**

| Error Type | Color Token | Hex (Light) | Visual Style |
|------------|-------------|-------------|--------------|
| Grammar | `--color-error` | `#ef5350` | Red underline wavy |
| Vocabulary | `--color-warning` | `#ff9800` | Orange underline dotted |
| Sentence structure | `--color-accent` | `#5b5bf0` | Blue underline dashed |
| Style/register | `--color-text-muted` | `#8a8aa0` | Gray highlight |
| Punctuation | `--color-warm` | `#e8a87c` | Warm underline dotted |
| Spelling | `--color-error` | `#ef5350` | Red strikethrough |
| Content/relevance | `--color-warning` | `#ff9800` | Orange highlight bg |

**Good language use highlighting:** `--color-success-bg` background, `--color-success` border.

**Part A vs Part B visual distinction (exam header/nav):**
- Part A: Use existing `--color-accent` badge pattern
- Part B option badges: Use `--color-warm` hue (similar to Part B1/B2 in Reading)

**Timer color states:**

| Time Remaining | Color |
|----------------|-------|
| > 30 min | `--color-text` (default) |
| ≤ 30 min | `--color-warning` |
| ≤ 15 min | `--color-warning` (flashing, 400ms animation) |
| ≤ 5 min | `--color-error` (flashing, 400ms animation) |

**Source:** Existing CSS tokens from `src/App.css` lines 17-61. Error type color mapping derived from CONTEXT.md D-16, D-23 specifics. Timer colors derived from Reading Module pattern and CONTEXT.md D-30.

### Visual Hierarchy — Primary Screen Focal Point

On the main writing exam screen, the **focal point hierarchy** is:

| Element | Priority | Role |
|---------|----------|------|
| RuledLineEditor | Primary (focal) | Center-screen essay area — largest surface, draws the eye |
| WritingExamHeader | Secondary (anchor) | Top banner — sets exam context frame |
| WritingTimer | Secondary (peripheral) | Top-right — glanceable status, not constant focus |
| WritingPromptCard | Tertiary (collapsible) | Left/above editor — referenced occasionally, not persistent focus |

No single element competes with the RuledLineEditor for visual attention. The editor occupies ~60% of viewport width (or full-width in distraction-free mode). The prompt card is dismissible to maximize editor prominence.

---

## Copywriting Contract

| Element | Copy | Source |
|---------|------|--------|
| Primary CTA (start) | "Submit for Correction" | Existing code — keep unchanged |
| Primary CTA (session select) | "Start Writing Practice" | Default — matches Reading "Start New Practice" pattern |
| Part B option header | "Choose ONE of the following tasks:" | DSE Paper 2 standard instruction |
| Empty state (no sessions) | "No writing sessions yet" / "Complete a practice session to see your history here." | Default — matches Reading history pattern |
| Empty state (no prompts available) | "No prompts available" / "Try adjusting your settings or check your connection." | Default |
| Error state (AI correction) | "Correction failed" / "The AI examiner encountered an error. You can try submitting again, or save your essay and try later." | Default — matches existing pattern |
| Timer warnings | "30 minutes remaining" / "15 minutes remaining" / "5 minutes remaining" | CONTEXT.md D-30 — specific time thresholds |
| Exam header title | "Hong Kong Diploma of Secondary Education Examination" | CONTEXT.md D-28 — match existing Reading exam header |
| Exam header subtitle | "English Language Paper 2 — Writing" | CONTEXT.md D-28 |
| Start screen card labels | "Part A (Compulsory)" / "Part B (Choose from options)" | New — distinct from Reading's B1/B2 split |
| Destructive confirmation | None in this phase | No delete/submit-without-confirmation flows |

**New copywriting for correction feedback:**

| Element | Copy |
|---------|------|
| Rubric score header | "HKEAA Marking Scheme" |
| Content category | "Content (7 marks)" |
| Organisation category | "Organisation (7 marks)" |
| Language category | "Language (7 marks)" |
| Error list heading | "Detailed Error Analysis" |
| Good language heading | "Good Language Use" |
| Vocabulary heading | "Vocabulary Upgrades" |
| Section breakdown heading | "Section-by-Section Breakdown" |
| Pitfalls avoided heading | "Common DSE Pitfalls You Avoided" |
| Cross-session heading | "Error Pattern Across Sessions" |
| Self-assessment prompt | "Which parts are you unsure about?" |
| Re-submit CTA | "Revise and Re-submit" |
| Comparison heading | "Session Comparison" |

**Source:** Existing copy from `WritingModule.jsx` lines 60-118 for correction prompt. CONTEXT.md D-16 through D-26 for feedback UI copy. CONTEXT.md D-27 through D-32 for exam environment copy.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not required |
| (none) | (none) | not applicable |

**Note:** Phase 4 does not introduce any third-party design system registries. All UI is implemented via custom CSS extending existing `writing__*` BEM patterns in `src/App.css`.

---

## Component Inventory

New components to be built in this phase, with their interaction contracts:

### 1. WritingStartScreen
- **States:** idle, generating, error
- **Interaction:** Show difficulty selection cards (matching Reading pattern) + Part A/Part B split button. Part B shows option count. "Resume session" banner if saved session exists.
- **Copywriting:** See Copywriting Contract above.
- **CSS namespace:** `.writing__start` (extend `.reading__start` pattern)

### 2. WritingPromptCard
- **States:** loading, ready (with text type badge)
- **Interaction:** Display full DSE prompt card: context paragraph, task instructions, bullet-point requirements, word limit, text type badge. No AI badge.
- **Copy:** From prompt bank JSON or AI generation.
- **CSS namespace:** `.writing__prompt-card` (extend existing `.writing__prompt-card`)

### 3. PartBOptionSelector
- **States:** loading, ready, empty
- **Interaction:** Grid of 4 option cards (post-2024 curriculum). Each shows: text type badge, title, short description. Click to select. (This is NEW — replaces the simple difficulty picker for Part B.)
- **Copy:** "Choose ONE of the following tasks:" header.
- **CSS namespace:** `.writing__part-b-options`

### 4. WritingExamHeader
- **States:** static
- **Interaction:** Display "HKDSE English Language Paper 2 — Writing", Seat No., Candidate No., exam year, instructions. Same pattern as `.reading__exam-framework`.
- **Copy:** See Copywriting Contract.
- **CSS namespace:** `.writing__exam-framework` (reuse `.reading__exam-framework` pattern)

### 5. WritingTimer
- **States:** running (HH:MM:SS), warning (≤30min), critical (≤15min), danger (≤5min), expired
- **Interaction:** Countdown timer. Warning color transitions at thresholds. Sound alert at 30min/15min/5min (configurable). Auto-submit at 0.
- **Copy:** None — numeric display only.
- **CSS namespace:** `.writing__timer` (extend `.reading__timer` pattern with HH:MM:SS format)

### 6. RuledLineEditor
- **States:** writing, auto-saving (subtle indicator), crash-recovered
- **Interaction:** ContentEditable with ruled line background. No word count display during exam mode. Auto-save every 30s to IndexedDB. Crash recovery restores last auto-saved draft. Full-screen distraction-free width.
- **Copy:** Placeholder: "Start writing your essay here..."
- **CSS namespace:** `.writing__editor--exam` (extend existing `.writing__editor`)

### 7. WritingSubmitButton
- **States:** enabled, disabled (below min word count), submitting
- **Interaction:** Located in editor footer. Must pass min word count check. Cannot submit during auto-save or correction in progress.
- **Copy:** "Submit for Correction" / "Submitting..."
- **CSS namespace:** `.writing__submit-btn` (existing)

### 8. WritingCorrectionResults
- **States:** loading, complete, error
- **Interaction:** Show rubric scores (Content/Organisation/Language × 7 marks each), DSE level, overall percentage, error list with inline annotations, good language use section, vocabulary upgrades, section breakdown, pitfalls avoided, error frequency chart, cross-session pattern.
- **Copy:** See Copywriting Contract for all sub-headings.
- **CSS namespace:** `.writing__correction` (new container)

### 9. InlineAnnotationLayer
- **States:** active (annotation), hover (tooltip)
- **Interaction:** Overlay error annotations directly on essay text with color-coded underlines/highlights. Hover shows correction tooltip. Click to scroll to error detail.
- **Copy:** Tooltip shows original → correction + explanation.
- **CSS namespace:** `.writing__annotation`

### 10. WritingErrorChart
- **States:** populated, empty
- **Interaction:** Bar chart showing error counts by category (grammar, vocab, structure, etc.) with color coding matching annotation colors.
- **Copy:** Category labels matching error types.
- **CSS namespace:** `.writing__error-chart`

### 11. WritingSectionBreakdown
- **States:** populated, empty
- **Interaction:** Per-section (introduction, body paragraphs, conclusion) scores with brief feedback.
- **Copy:** Section name + score breakdown.
- **CSS namespace:** `.writing__section-breakdown`

### 12. WritingSelfAssessment
- **States:** collapsed (default), expanded
- **Interaction:** Before submission, student tags areas they're unsure about. Tags sent along with essay for correction. Simple tag chip interface.
- **Copy:** "Which parts are you unsure about?" prompt.
- **CSS namespace:** `.writing__self-assessment`

### 13. WritingResubmitPanel
- **States:** hidden (default feedback), visible (after first feedback)
- **Interaction:** After receiving feedback, "Revise and Re-submit" button appears. Re-submission sends revised essay + previous correction context for comparison.
- **Copy:** "Revise and Re-submit" / "Comparing..." / "Comparison"
- **CSS namespace:** `.writing__resubmit`

### 14. WritingSessionHistory
- **States:** loading, populated, empty, error
- **Interaction:** Browseable list of past writing sessions. Each entry shows: date, prompt title, text type, DSE level, overall score. Click to view full correction. Side-by-side comparison for two selected sessions.
- **Copy:** "No writing sessions yet." / "Compare Sessions" button.
- **CSS namespace:** `.writing__history` (extend `.reading__history` pattern)

---

## Interaction Patterns

### Phase State Machine
```
start → [select part A] → writing-exam → [submit] → correcting → correction-results
      → [select part B] → choose-option → writing-exam → ...
```

The Writing Module uses the same internal phase state machine pattern as Reading Module (CONTEXT.md — established patterns):
- `start` → Difficulty/Part selection
- `choosing` (Part B only) → Option selection
- `writing` → Exam mode with timer
- `correcting` → AI correction in progress
- `correction` → Feedback/results display
- `history` → Session history browse
- `comparison` → Side-by-side comparison

### Distraction-Free Mode
When in `writing` phase AND exam mode (after timer starts):
- Sidebar nav hidden (`display: none` on `.app__nav`)
- Context panel hidden (`display: none` on `.app__panel`)
- Full-width editor + prompt card
- Implemented via existing `.app--focus` class (already toggled in ViewContext)

### Auto-Save Pattern
- Timer: 30 seconds (CONTEXT.md D-34)
- Storage: IndexedDB via `useIndexedDB` with key `writing-session-{sessionId}`
- On mount: check for auto-saved draft → show resume option
- Indicator: Subtle "Auto-saving..." → "Saved" text in editor footer (fades out after 2s)

### Annotation Hover/Tooltip
- Error underline/highlight in essay text
- Hover: Show tooltip with: `[Error Type]` → original text → correction → explanation
- Click: Scroll error detail panel to matching error entry
- Implemented as absolutely positioned tooltip with `:hover` CSS trigger + React state for click-to-scroll

### Sound Alert Pattern
- At 30min, 15min, 5min thresholds
- Configurable on/off toggle (in settings or module header)
- Implementation: Use `new Audio()` for simple beep (no HTML Audio element needed — agent discretion from CONTEXT.md)
- Store preference in localStorage: `crescendo-writing-sound-alerts` (boolean)

### Responsive Breakpoints
Match existing Reading Module breakpoints exactly (CONTEXT.md D-33):

| Breakpoint | Behavior |
|------------|----------|
| > 1024px | Full two-column: prompt panel (380px) + editor (flex) |
| ≤ 1024px | Stack vertically: prompt card above editor; history grid 2-col |
| ≤ 834px | Single column: prompt card collapsible; error chart stacks blocks |
| ≤ 768px | Compact: smaller padding, timer smaller, editor full-width |
| ≤ 480px | Minimal: annotation text hidden, tooltips only; single-column everything |

---

## CSS Architecture

All new styles go into `src/App.css` under a new section header:
```css
/* ===== Writing Module — Phase 4 Overhaul ===== */
```

BEM naming convention (extending existing `writing__*` pattern):
- **Block:** `.writing` — no top-level block needed (use `.writing__*` directly as existing code does)
- **Element:** `.writing__start`, `.writing__part-b-options`, `.writing__exam-framework`, `.writing__timer`, `.writing__editor--exam`, `.writing__correction`, `.writing__annotation`, `.writing__error-chart`, `.writing__section-breakdown`, `.writing__self-assessment`, `.writing__resubmit`, `.writing__history`
- **Modifier:** `.writing__timer--warning`, `.writing__timer--critical`, `.writing__timer--danger`, `.writing__editor--exam`, `.writing__annotation--grammar`, `.writing__annotation--vocabulary`, `.writing__annotation--structure`, etc.

Reuse existing CSS custom properties exclusively. No new color/spacing/typography tokens — use the defined variables.

**Important:** Do NOT modify existing `.reading__*` or `.dse-module__*` styles. Extend them with writing-specific overrides only where necessary.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
