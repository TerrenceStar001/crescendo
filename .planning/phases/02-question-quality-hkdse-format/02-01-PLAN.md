---
phase: 02-question-quality-hkdse-format
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/questionTypes.js
  - src/components/QuestionRenderer.jsx
autonomous: true
requirements:
  - READ-02
  - READ-07
must_haves:
  truths:
    - "Reading sessions generate questions with per-part type distributions matching real DSE proportions"
    - "Summary Cloze, Pronoun Reference, and Semantic Matching questions render with proper interactive inputs"
    - "Each part totals 42 marks with 19-27 questions"
    - "MCQ distractors use one of 5 documented cognitive trap categories"
  artifacts:
    - path: "src/utils/questionTypes.js"
      provides: "Question type distribution config, mark allocation, cognitive trap taxonomy"
      min_lines: 150
      exports: ["QUESTION_TYPE_DISTRIBUTIONS", "MARK_ALLOCATION", "COGNITIVE_TRAP_TYPES", "QUESTION_CATEGORIES", "TYPE_DISTRIBUTION"]
    - path: "src/components/QuestionRenderer.jsx"
      provides: "Input components for 3 new DSE question types"
      contains: "SummaryClozeInput, PronounRefInput, SemanticConnectInput"
  key_links:
    - from: "src/hooks/useDSEPapers.js"
      to: "src/utils/questionTypes.js"
      via: "import of QUESTION_TYPE_DISTRIBUTIONS"
      pattern: "import.*questionTypes"
    - from: "src/components/QuestionRenderer.jsx"
      to: "question.type === 'summary-cloze'|'pronoun-ref'|'semantic-connect'"
      via: "switch-case rendering"
      pattern: "summary-cloze|pronoun-ref|semantic-connect"
---

<objective>
Configure DSE Paper 1 question type distributions per part, mark allocation (42 marks), cognitive trap taxonomy (5 types), and render input components for all 9 HKDSE question types.

Purpose: Phase 1 established passage generation. This plan gives the system awareness of DSE question type proportions and the UI to render every type a real DSE paper uses.

Output:
- `src/utils/questionTypes.js` — distribution config, mark allocation, cognitive traps, question difficulty categories
- `src/components/QuestionRenderer.jsx` — 3 new input sub-components and switch-case wiring
</objective>

<execution_context>
@/home/terrence/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/terrence/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-question-quality-hkdse-format/02-RESEARCH.md
@.planning/phases/02-question-quality-hkdse-format/02-PATTERNS.md
@/home/terrence/珈珈/Code/Crescendo/src/utils/structuralConstraints.js
@/home/terrence/珈珈/Code/Crescendo/src/components/QuestionRenderer.jsx
@/home/terrence/珈珈/Code/Crescendo/src/hooks/useDSEPapers.js

<interfaces>
From `src/utils/structuralConstraints.js` (pattern analog):
```javascript
export const WORD_COUNT_TARGETS = {
  B1: { label: 'Part B1 — Easier', min: 600, max: 1000, textCount: '2-4' },
  A:  { label: 'Part A — Compulsory', min: 900, max: 1200, textCount: '1-2' },
  B2: { label: 'Part B2 — Harder', min: 1000, max: 1200, textCount: '1-2' },
};
export function getMaxTokensForPart(part) { ... }
```

From `src/components/QuestionRenderer.jsx` (current switch statement, lines 274-281):
```javascript
switch (type) {
  case 'gap-fill': input = <GapFillInput ... />; break;
  case 'tfng': input = <TFNGInput ... />; break;
  case 'short-answer': input = <ShortAnswerInput ... />; break;
  case 'matching': input = <MatchingInput ... />; break;
  case 'open-ended': input = <OpenEndedInput ... />; break;
  default: input = <MCQInput ... />;
}
```

TYPE_LABELS (lines 3-10):
```javascript
const TYPE_LABELS = {
  mcq: 'Multiple Choice', 'gap-fill': 'Fill in the Blank',
  tfng: 'True / False / Not Given', 'short-answer': 'Short Answer',
  matching: 'Matching', 'open-ended': 'Open-ended',
};
```

From `src/hooks/useDSEPapers.js`, VALID_TYPES (line 174):
```javascript
const VALID_TYPES = new Set(['mcq', 'tfng', 'gap-fill', 'short-answer', 'matching', 'open-ended', 'summary-cloze', 'pronoun-ref', 'semantic-connect']);
```

Research distribution percentages per part (from 02-RESEARCH.md):
- Short answer / factual info: 25-35% (all parts)
- Cloze / gap-fill: 15-25% (all parts)
- MCQ: 10-20% (all parts)
- Matching: 8-15% (B1 heavier, B2 lighter)
- TFNG: 8-12% (all parts)
- Reference questions: 5-10% (B2 heavier)
- Table/chart completion: 5-10% (B1 heavier)
- Inferencing / opinion: 5-10% (B2 only)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create questionTypes.js — DSE question type config, mark allocation, cognitive trap taxonomy</name>
  <files>src/utils/questionTypes.js</files>
  <read_first>
    src/utils/structuralConstraints.js — pattern analog for constants export style (UPPER_SNAKE_CASE, camelCase helpers, per-part grouping)
    .planning/phases/02-question-quality-hkdse-format/02-RESEARCH.md — distribution percentages, mark patterns, distractor types
    .planning/phases/02-question-quality-hkdse-format/02-PATTERNS.md — the exact export template for questionTypes.js
  </read_first>
  <action>
    Create `src/utils/questionTypes.js` following the exact pattern from `structuralConstraints.js`:
    - Named exports of UPPER_SNAKE_CASE constants
    - camelCase helper functions
    - Descriptive comment block at file top

    Required exports:
    1. `QUESTION_TYPE_DISTRIBUTIONS` — object keyed by part `'A'`, `'B1'`, `'B2'`. Each value is an object of type slugs mapped to percentage ranges. Types: `short-answer`, `cloze`, `mcq`, `matching`, `tfng`, `reference`, `table-chart`, `inferencing`, `summary-cloze`, `sequencing`, `proofreading`, `pronoun-ref`, `semantic-connect`. Use the percentage ranges from RESEARCH.md. B2 gets inferencing at 5-10%; B1 gets table-chart at 5-10% with no inferencing. All ranges should be valid percentages that sum to ~100% per part.

    2. `MARK_ALLOCATION` — object with:
       - `marksPerPart: 42`
       - `questionRange: { min: 19, max: 27 }`
       - `defaultMarks: 1`
       - `multiMarkTypes: ['open-ended', 'short-answer']` — types that sometimes carry 2-3 marks

    3. `COGNITIVE_TRAP_TYPES` — array of 5 objects per D-04:
       - `{ id: 'extreme-wording', label: 'Extreme Wording', description: 'Uses always/never/all/none/every to overgeneralize', example: '...' }`
       - `{ id: 'similar-sounding', label: 'Similar Sounding', ... }`
       - `{ id: 'partial-truth', label: 'Partial Truth', ... }`
       - `{ id: 'out-of-scope', label: 'Out of Scope', ... }`
       - `{ id: 'numerical', label: 'Numerical Distraction', ... }`
       NO opposite traps (per D-04).

    4. `QUESTION_CATEGORIES` — object keyed by difficulty band: `'foundation'` (Level 2-4), `'intermediate'` (Level 4-5), `'advanced'` (Level 5), `'highest'` (Level 5**). Each value is an array of type slugs from RESEARCH.md question type taxonomy.

    5. `DIFFICULTY_PROGRESSION_BY_PART` — object keyed by part (A/B1/B2). Each value has percentage breakdown by difficulty band from RESEARCH.md:
       - Part A: 70% foundation, 20% intermediate, 10% advanced
       - Part B1: 80% foundation, 20% intermediate
       - Part B2: 20% intermediate, 40% advanced, 40% highest

    6. `getTypeDistributionForPart(part)` — helper that returns `{ typeSlug: percentage }` by picking specific percentages within the RESEARCH.md ranges. Use the midpoint of each range as default.

    7. `getMarkAllocationForPart(part)` — helper that returns `{ marksPerPart: 42, questionRange: { min: 19, max: 27 } }`.

    8. `getCognitiveTrapForType(trapId)` — helper returning the trap object by id.

    Per D-02: total marks = exactly 42 per part. Per D-03: all HKDSE question types listed. Per D-04: 5 cognitive trap categories (no opposite traps).
  </action>
  <verify>
    <automated>node -e "import('./src/utils/questionTypes.js').then(m => { console.log('QUESTION_TYPE_DISTRIBUTIONS:', Object.keys(m.QUESTION_TYPE_DISTRIBUTIONS)); console.log('MARK_ALLOCATION:', m.MARK_ALLOCATION); console.log('COGNITIVE_TRAP_TYPES:', m.COGNITIVE_TRAP_TYPES.length); console.log('QUESTION_CATEGORIES:', Object.keys(m.QUESTION_CATEGORIES)); })" 2>&1 | head -20</automated>
  </verify>
  <done>
    questionTypes.js exports all 8 constants/helpers. QUESTION_TYPE_DISTRIBUTIONS is keyed by part (A/B1/B2). COGNITIVE_TRAP_TYPES has exactly 5 items (no opposite). MARK_ALLOCATION.marksPerPart === 42. DIFFICULTY_PROGRESSION_BY_PART has A/B1/B2 entries.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add 3 new input sub-components to QuestionRenderer.jsx and wire them into the switch</name>
  <files>src/components/QuestionRenderer.jsx</files>
  <read_first>
    src/components/QuestionRenderer.jsx — full file (read all 347 lines). Pay special attention to:
      - TYPE_LABELS object (lines 3-10) — add 3 new entries
      - Existing sub-component patterns (MCQInput, GapFillInput, TFNGInput, etc.) — match their prop interface: { question, value, onSelect, showResult, disabled }
      - switch statement (lines 274-281) — add 3 new cases
      - isQuestionCorrect export (lines 297-347) — add cases for 3 new types
    .planning/phases/02-question-quality-hkdse-format/02-PATTERNS.md — TYPE_LABELS template and switch additions
  </read_first>
  <action>
    Add three new entries to TYPE_LABELS:
    - `'summary-cloze': 'Summary Cloze'`
    - `'pronoun-ref': 'Reference Question'`
    - `'semantic-connect': 'Semantic Matching'`

    Add three new sub-components BEFORE the default export, following the exact prop pattern `{ question, value, onSelect, showResult, disabled }` used by existing sub-components:

    1. **SummaryClozeInput** — Renders an inline stem with numbered blanks. The `question.stem` contains summary text with blank markers like `{1}`, `{2}` etc. Split the stem on `{\d+}` pattern to interleave text spans and input fields. Each input field is a small inline `<input type="text">`. `question.answers` is an array of correct answers (or `question.correctAnswer` as comma-separated). Show result state: mark each blank green/red with correct answer shown below wrong ones.

    2. **PronounRefInput** — Renders stem (e.g., "What does 'it' in line 12 refer to?"). If `question.options` exists, render as MCQ-style buttons. If no options, render as short-answer textarea. Show result: show correct answer + explanation with passage reference.

    3. **SemanticConnectInput** — Renders a cause-effect or claim-evidence matching grid. Uses `question.pairs` (cause items in column A) and dropdown selects for effect items (column B). Matches existing MatchingInput pattern but the grid has semantic labels: "Cause" / "Effect" or "Claim" / "Evidence". Show result: green/red per row with correct match shown.

    Use the same CSS classes as existing sub-components: `.mcq__stem`, `.mcq__option`, `.mcq__option--correct`, `.mcq__option--wrong`, `.gf__input`, `.gf__input--correct`, `.gf__input--wrong`, `.sa__textarea`, `.matching__grid`, `.matching__row`, `.matching__select`.

    Add 3 new cases to the switch statement:
    ```
    case 'summary-cloze': input = <SummaryClozeInput ... />; break;
    case 'pronoun-ref': input = <PronounRefInput ... />; break;
    case 'semantic-connect': input = <SemanticConnectInput ... />; break;
    ```

    Update exported `isQuestionCorrect` function (lines 297-347): add cases for:
    - `'summary-cloze'`: match against `question.answers` array (currently uses same logic as gap-fill multi-blank)
    - `'pronoun-ref'`: match single answer string
    - `'semantic-connect'`: match using `question.pairs` (same logic as matching)

    Do NOT touch existing components, styling, or logic. Add only the 3 new components, TYPE_LABELS entries, switch cases, and isQuestionCorrect cases.
  </action>
  <verify>
    <automated>node -e "
const fs = require('fs');
const content = fs.readFileSync('src/components/QuestionRenderer.jsx', 'utf8');
const checks = [
  ['SummaryClozeInput', content.includes('function SummaryClozeInput')],
  ['PronounRefInput', content.includes('function PronounRefInput')],
  ['SemanticConnectInput', content.includes('function SemanticConnectInput')],
  ['summary-cloze type label', content.includes(\"'summary-cloze': 'Summary Cloze'\")],
  ['pronoun-ref type label', content.includes(\"'pronoun-ref': 'Reference Question'\")],
  ['semantic-connect type label', content.includes(\"'semantic-connect': 'Semantic Matching'\")],
  ['summary-cloze switch case', content.includes(\"case 'summary-cloze':\")],
  ['pronoun-ref switch case', content.includes(\"case 'pronoun-ref':\")],
  ['semantic-connect switch case', content.includes(\"case 'semantic-connect':\")],
];
checks.forEach(([name, ok]) => console.log(ok ? 'PASS' : 'FAIL', '-', name));
console.log('All:', checks.every(c => c[1]) ? 'PASS' : 'FAIL');
"</automated>
  </verify>
  <done>
    QuestionRenderer.jsx exports 3 new input sub-components. TYPE_LABELS has 9 entries. switch statement handles all 9 types. isQuestionCorrect handles all 9 types.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AI content → QuestionRenderer | AI-generated question JSON parsed and rendered as interactive inputs. Text content in stems/options rendered as React text children. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | QuestionRenderer.jsx | mitigate | AI-generated question stems are rendered as React text content (`.mcq__stem`, etc.) — no dangerouslySetInnerHTML. React's built-in JSX escaping prevents XSS from AI output. |
| T-02-02 | Tampering | questionTypes.js | accept | Static config file with no runtime data input. No user-controllable values. Low-value target. |
| T-02-SC | Tampering | npm installs | mitigate | No new npm packages added in this plan. |
</threat_model>

<verification>
1. `node -e "import('./src/utils/questionTypes.js').then(m => console.log(Object.keys(m)))"` shows all 8 exports
2. `node` text verification of QuestionRenderer.jsx confirms 3 new component functions, TYPE_LABELS entries, and switch cases
3. `npm run build` passes by: `NODE_OPTIONS="--experimental-global-webcrypto" npx vite build 2>&1 | tail -5`
</verification>

<success_criteria>
- questionTypes.js created with all required exports
- Build passes
- ReadingModule (via useDSEPapers) can now generate questions using type distributions
- Summary Cloze, Pronoun Reference, and Semantic Matching questions render with proper inputs
</success_criteria>

<output>
Create `.planning/phases/02-question-quality-hkdse-format/02-01-SUMMARY.md` when done
</output>
